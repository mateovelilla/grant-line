{
  "name": "grant-line",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf pnpm-lock.yaml && rm -rf node_modules && pnpm -r exec rm -rf node_modules && pnpm -r exec rm -rf tsconfig.tsbuildinfo && pnpm -r exec rm -rf tsconfig.tsbuildinfo && pnpm recursive exec rm -rf dist",
    "build": "pnpm -r build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.14.0",
  "engines": {
    "node": "22",
    "pnpm": ">=3"
  },
  "devDependencies": {
    "@biomejs/biome": "2.1.4",
    "@types/node": "^24.2.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "tsx": "^4.20.3"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
